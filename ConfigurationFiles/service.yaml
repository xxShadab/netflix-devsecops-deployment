apiVersion: v1
kind: Service
metadata:
  name: netflix-app
  labels:
    app: netflix-app
spec:
  ports:
  - name: http
    port: 30008
    protocol: TCP
    targetPort: 30008
  selector:
    app: netflix-app
  type: NodePort
